/*package com.avnet.emasia.webquote.web.quote.vo;

commented  By DamonChen@20171219
import java.util.List;
import java.util.logging.Logger;

import javax.faces.model.ListDataModel;
import org.primefaces.model.SelectableDataModel;

import com.avnet.emasia.webquote.vo.PricerInfo;
import com.avnet.emasia.webquote.web.quote.vo.SearchPartVO;



public class PartPopupModel extends ListDataModel<PricerInfo> implements SelectableDataModel<PricerInfo> , java.io.Serializable{  

	*//**
	 * 
	 *//*
	private static final long serialVersionUID = 1L;
	static Logger logger = Logger.getLogger("PartPopupModel");
	public PartPopupModel() {
    }

    public PartPopupModel(List<PricerInfo> data) {
        super(data);
    }
    
    @Override
    public PricerInfo getRowData(String rowKey) {
        //In a real app, a more efficient way like a query by rowKey should be implemented to deal with huge data
        List<PricerInfo> searchPartVOs = (List<PricerInfo>) getWrappedData();
        
        for(PricerInfo pm : searchPartVOs) {
            if(String.valueOf(pm.getPricerId()).equals(rowKey))
                return pm;
        }
        
        return null;
    }

    @Override
    public Object getRowKey(PricerInfo pm) {
        return pm.getPricerId();
    }
}
                    */